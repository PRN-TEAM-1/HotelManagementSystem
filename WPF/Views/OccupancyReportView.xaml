<UserControl x:Class="WPF.Views.OccupancyReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Occupancy Report"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">

            <TextBlock Text="Start Date:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <DatePicker SelectedDate="{Binding StartDate}"
                        Width="140"
                        Margin="0,0,16,0"/>

            <TextBlock Text="End Date:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <DatePicker SelectedDate="{Binding EndDate}"
                        Width="140"
                        Margin="0,0,16,0"/>

            <Button Content="Filter"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding FilterCommand}"/>

            <Button Content="Export CSV"
                    Width="110"
                    Command="{Binding ExportCommand}"/>

            <TextBlock Margin="20,0,0,0"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Text="{Binding TotalRoomNights, StringFormat=Total Room Nights: {0}}"/>

        </StackPanel>

        <TextBlock Grid.Row="2"
                   Text="{Binding Message}"
                   Foreground="Red"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding OccupancyReports}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Number"
                                    Binding="{Binding RoomNumber}"
                                    Width="*"/>

                <DataGridTextColumn Header="Room Type"
                                    Binding="{Binding RoomType}"
                                    Width="2*"/>

                <DataGridTextColumn Header="Total Nights Booked"
                                    Binding="{Binding TotalNightsBooked}"
                                    Width="*"/>

                <DataGridTextColumn Header="Occupancy Rate (%)"
                                    Binding="{Binding OccupancyRate}"
                                    Width="*"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>