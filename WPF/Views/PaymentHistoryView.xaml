<UserControl x:Class="WPF.Views.PaymentHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <materialDesign:Card Padding="16"
                         materialDesign:ElevationAssist.Elevation="Dp2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Payment History"
                       Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                       Foreground="{StaticResource TextPrimaryBrush}" />

            <Border Grid.Row="1"
                    Padding="10"
                    Margin="0,10,0,0"
                    Background="#FFF3D6"
                    CornerRadius="8"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AlertCircleOutline"
                                             Width="18"
                                             Height="18"
                                             Foreground="{StaticResource WarningBrush}"
                                             Margin="0,0,8,0" />
                    <TextBlock Text="{Binding ErrorMessage}"
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Foreground="#856404"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <DataGrid ItemsSource="{Binding Payments}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Method" Binding="{Binding PaymentMethod}" Width="Auto" MinWidth="100" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='{}{0:N0} VND'}" Width="*" />
                        <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="Auto" MinWidth="132" />
                        <DataGridTextColumn Header="Receiver" Binding="{Binding ReceivedByUserName}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto" MinWidth="80" />
                        <DataGridTextColumn Header="Transaction" Binding="{Binding TransactionCode}" Width="*" />
                        <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="1.2*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Border Padding="16"
                        Background="{StaticResource SurfaceMutedBrush}"
                        CornerRadius="8"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding EmptyMessage, Converter={StaticResource StringToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="History"
                                                 Width="22"
                                                 Height="22"
                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                 Margin="0,0,8,0" />
                        <TextBlock Text="{Binding EmptyMessage}"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>
