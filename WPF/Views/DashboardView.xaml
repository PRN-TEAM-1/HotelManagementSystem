<UserControl x:Class="WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WPF.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,12" LastChildFill="True">
            <Button DockPanel.Dock="Right"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="Refresh dashboard">
                <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" />
            </Button>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ViewDashboard"
                                         Width="20"
                                         Height="20"
                                         Foreground="{StaticResource AccentBrush}"
                                         Margin="0,0,8,0" />
                <TextBlock Text="Today"
                           Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                           Foreground="{StaticResource TextPrimaryBrush}" />
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <ui:Card Padding="16"
                         Margin="0,0,0,12"
                         Style="{StaticResource FluentUiCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="OfficeBuilding"
                                                     Width="20"
                                                     Height="20"
                                                     Foreground="{StaticResource AccentBrush}"
                                                     Margin="0,0,8,0" />
                            <TextBlock Text="Room Summary"
                                       Style="{StaticResource SectionTitleTextStyle}" />
                        </StackPanel>

                        <UniformGrid Grid.Row="1"
                                     Columns="3"
                                     Margin="0,12,0,0">
                            <controls:DashboardCardControl Title="Total Rooms"
                                                           Value="{Binding Dashboard.RoomSummary.TotalRooms}" />
                            <controls:DashboardCardControl Title="Available"
                                                           Value="{Binding Dashboard.RoomSummary.AvailableRooms}" />
                            <controls:DashboardCardControl Title="Occupied"
                                                           Value="{Binding Dashboard.RoomSummary.OccupiedRooms}" />
                            <controls:DashboardCardControl Title="Reserved"
                                                           Value="{Binding Dashboard.RoomSummary.ReservedRooms}" />
                            <controls:DashboardCardControl Title="Cleaning"
                                                           Value="{Binding Dashboard.RoomSummary.CleaningRooms}" />
                            <controls:DashboardCardControl Title="Maintenance"
                                                           Value="{Binding Dashboard.RoomSummary.MaintenanceRooms}" />
                        </UniformGrid>
                    </Grid>
                </ui:Card>

                <ui:Card Padding="16"
                         Style="{StaticResource FluentUiCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ReceiptText"
                                                     Width="20"
                                                     Height="20"
                                                     Foreground="{StaticResource AccentBrush}"
                                                     Margin="0,0,8,0" />
                            <TextBlock Text="Operations"
                                       Style="{StaticResource SectionTitleTextStyle}" />
                        </StackPanel>

                        <UniformGrid Grid.Row="1"
                                     Columns="3"
                                     Margin="0,12,0,0">
                            <controls:DashboardCardControl Title="New Bookings"
                                                           Value="{Binding Dashboard.BookingSummary.TodayBookings}" />
                            <controls:DashboardCardControl Title="Active Stays"
                                                           Value="{Binding Dashboard.TodayOperation.ActiveStays}" />
                            <controls:DashboardCardControl Title="Due Arrivals"
                                                           Value="{Binding Dashboard.TodayOperation.DueArrivals}" />
                            <controls:DashboardCardControl Title="Due Departures"
                                                           Value="{Binding Dashboard.TodayOperation.DueDepartures}" />
                            <controls:DashboardCardControl Title="Completed Check-ins"
                                                           Value="{Binding Dashboard.TodayOperation.TodayCheckIns}" />
                            <controls:DashboardCardControl Title="Completed Check-outs"
                                                           Value="{Binding Dashboard.TodayOperation.TodayCheckOuts}" />
                        </UniformGrid>
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
