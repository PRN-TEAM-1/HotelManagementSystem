<UserControl x:Class="WPF.Views.SectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <materialDesign:Card Padding="24"
                                 Background="{StaticResource HeroBrush}"
                                 materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding AccentLabel}"
                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                   FontWeight="SemiBold"
                                   Foreground="#DDECF3" />
                        <TextBlock Text="{Binding Title}"
                                   Margin="0,8,0,0"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   FontFamily="{StaticResource AppDisplayFont}"
                                   Foreground="{StaticResource TextInverseBrush}" />
                        <TextBlock Text="{Binding Description}"
                                   Margin="0,8,0,0"
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   Foreground="#E3F1F7"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <Border Grid.Column="1"
                            Width="64"
                            Height="64"
                            Margin="24,0,0,0"
                            CornerRadius="16"
                            Background="#22FFFFFF">
                        <materialDesign:PackIcon Kind="ViewDashboard"
                                                 Width="34"
                                                 Height="34"
                                                 Foreground="{StaticResource TextInverseBrush}"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center" />
                    </Border>
                </Grid>
            </materialDesign:Card>

            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <materialDesign:Card Padding="16"
                                     Margin="0,0,8,0"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="Overview"
                                   Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <ItemsControl ItemsSource="{Binding Highlights}" Margin="0,12,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Kind="CheckCircleOutline"
                                                                 Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource AccentBrush}"
                                                                 VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding}"
                                                   Margin="8,0,0,0"
                                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   TextWrapping="Wrap" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1"
                                     Padding="16"
                                     Margin="8,0,0,0"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="Notes"
                                   Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <ItemsControl ItemsSource="{Binding HandoffNotes}" Margin="0,12,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Kind="InformationOutline"
                                                                 Width="18"
                                                                 Height="18"
                                                                 Foreground="{StaticResource TextSecondaryBrush}"
                                                                 VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding}"
                                                   Margin="8,0,0,0"
                                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   TextWrapping="Wrap" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>

            <materialDesign:Card Padding="16"
                                 Margin="0,16,0,0"
                                 materialDesign:ElevationAssist.Elevation="Dp1">
                <ItemsControl ItemsSource="{Binding Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,8,8"
                                    Padding="10,4"
                                    Background="{StaticResource AccentSoftBrush}"
                                    CornerRadius="8">
                                <TextBlock Text="{Binding}"
                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontWeight="SemiBold" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
